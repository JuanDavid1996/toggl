const common = require("./common");
const ProjectMiddleware = require("./ProjectMiddleware");
const TaskMiddleware = require("./TaskMiddleware");
const TrackerMiddleware = require("./TrackerMiddleware");

module.exports = {
    common,
    ProjectMiddleware,
    TaskMiddleware,
    TrackerMiddleware,
}